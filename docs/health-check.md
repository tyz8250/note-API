# Issue 32 で学んだこと

`GET /healthz` を追加して、アプリが起動して応答できるかを確認できるようにした。

このエンドポイントは業務データを返すためのものではなく、
「アプリが生きているか」を外から確認するために必要である。

開発中は `curl` で疎通確認に使え、
今後は監視やデプロイ確認の基礎にもなると理解した。

## 詰まったこと

health check は何のためにあるのか最初は曖昧だった。
でも実装してみて、APIの機能確認とは別に、
「まずサーバーが応答できるか」を見るためのものだとわかった。
